<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main Application Theme -->
    <style name="Theme.SpendBillionaireMoney" parent="android:Theme.Material.Light.NoActionBar">
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_color</item>

        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background_color</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>

        <item name="postSplashScreenTheme">@style/Theme.SpendBillionaireMoney</item>
    </style>

</resources>
